- hosts: depi
  become: yes

  tasks:
    - name: install docker
      apt:
        name: docker.io
        state: present
        update_cache: yes

    - name: start docker
      service:
        name: docker
        state: started
        enabled: yes